# The build context is the solution root; only the sources and the MSBuild
# props files are needed to publish a service image.
**/bin/
**/obj/
**/.vs/
**/.vscode/
**/*.user
.git/
.run/
docs/
deploy/
README.md
ARCHITECTURE.md
docker-compose.yml
